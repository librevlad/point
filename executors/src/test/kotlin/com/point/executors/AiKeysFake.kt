package com.point.executors

import com.point.core.flow.AiReadiness

/**
 * Готовность ключа AI для проверок (#529).
 *
 * Названа, а не подставлена умолчанием: без ключа имя действия договаривает «· нужен ключ», и
 * тест, сверяющий имя или порядок пузырьков, показывал бы тогда не способность, а состояние
 * настроек. Пусть каждая проверка говорит вслух, из какого мира она смотрит.
 */
internal val aiKeysReady = AiReadiness { true }

/** Ключа нет — для проверок ровно про то, как это видно человеку до тапа. */
internal val aiKeysMissing = AiReadiness { false }
