# Корпус реальности и метрика «действие без правок» (#262)

Качество Point судится не тестами, которые мы придумали, а тем, что приходит из реальности:
684 юнит-теста и при этом один живой прогон даёт по 3–7 дефектов. Поэтому есть корпус —
фиксированный набор **настоящих** кадров владельца, и одно число по нему.

Документ читается сверху вниз: сводка «где мы сейчас» → карта кадров → как устроена метрика →
летопись замеров. **Замеры вниз убраны намеренно:** их числа принадлежат своему прогону и задним
числом не переписываются, поэтому свежее состояние живёт в сводке, а не в последнем разделе.

## Где мы сейчас (сводка)

Величин четыре, и они не складываются в одну: это четыре разных обещания человеку.

| величина | что обещает человеку | последнее записанное число | где замер |
|---|---|---|---|
| **Действие без правок, без сети** | Point понял сам, на устройстве, бесплатно | **2 из 14** (04.08.2026, эмулятор; число занижено самим стендом — разбор в замере) | «Пятый замер» |
| **То же, после одного явного тапа в облако** | не смог сам, но честно предложил путь — и путь работает | приборы **2 из 3** на пяти кадрах, не взятых офлайн (01.08.2026) | «Вторая величина измерена» |
| **Таблица: не соврал ли файл** | значения в таблице — те, что в документе, а расхождения не молчат | пять размеченных кадров, 04.08.2026: **верен один файл из пяти**; на ведомости 36 из 49 сверенных ячеек, 3 молчаливых расхождения | «Пятый замер» |
| **Таблица: годится ли файл** (новая, #493) | человек получил таблицу, а не задание перепроверить её | пять размеченных кадров, 04.08.2026: взять как есть можно **от 18% до 93% листа**, годен **один файл из пяти** | «Пятый замер» |

Что сейчас в знаменателе:

- **Кадров в корпусе — 23.** Схему готовности имеют **14**, вне счёта — **9**. Знаменатель `14`
  впервые измерен целиком: четвёртый замер (03.08.2026) прошёл ровно по кадрам со схемой, включая
  три поздние — «деньги, квитанция и ответ», — которые третий замер судить ещё не мог.
- **Удаление кнопки «Прочитать показание» (04.08.2026, #396) знаменатель не изменило — ни на
  кадр.** Записано здесь потому, что выглядит оно как изменение: у продукта убрали действие, чьё
  имя (`meter-ocr`) похоже на схему кадров счётчиков (`meter-reading`). Это разные вещи. Удалённое
  действие резало табло из кадра и читало его цифровым словарём, а факт `entity.meter` **не писало
  вовсе**; готовность кадров 09/15/17 считалась и считается по факту, который пишет правило формы
  `meterFacts` поверх распознанного текста. Харнесс корпуса удалённого действия не звал никогда.
  Поэтому: **23 кадра, 14 со схемой, 9 вне счёта — те же числа**; 09, 15 и 17 остаются в счёте со
  схемой `meter-reading`, причина «вне счёта» им не заводится.
  **Убрать их из знаменателя было бы не честнее, а наоборот:** они считаются **неготовыми**, и
  вынос неготовых кадров поднял бы долю с 7 из 14 до 7 из 11 — метрика выросла бы от удаления
  работы.
- **Чем эти три кадра обслуживаются теперь — задачей #426, а не действием.** Сказать «показание
  приходит обычным распознаванием» было бы неправдой: прогон настоящих кадров через внешний глаз
  (04.08.2026, `docs/VISION-MODELS.md`) даёт **по приборам ноль из трёх** — показания в ответе нет
  вовсе, зато есть числа с шильдика, и правило «взять число рядом с единицей учёта» возьмёт именно
  их. То есть **сегодня в Point нет ни одного работающего пути от фотографии прибора к показанию**:
  был плохой (кнопка попадала в барабан один раз из трёх) — стало никакого. И это верно уже с
  внешним глазом в обычной цепочке чтения (#280/#429): «ноль из трёх» — не про отсутствие зрячей
  модели, а про то, что модель, которой дают страницу целиком, барабан не видит. Отвечающий у кадров
  09/15/17 назван и заведён: **#426** — читать прибор вырезанным барабаном с увеличением, а не
  страницей целиком. Разбор — в `DECISIONS.md`, «Кнопка «Прочитать показание» убрана».
- **Табличных кадров — 7**, и в долю готовности они не входят вовсе: у таблицы схемы готовности
  не будет (разбор — в «Как это меряется»). Эталон, размеченный человеком, есть у **пяти** (01,
  04, 06, 18, 23); кадры 10 и 19 эталона не имеют, и причины названы поимённо.
- **Вне счёта — не «забыто».** Каждый кадр вне знаменателя назван **вместе с причиной**: либо он
  табличный и меряется своей метрикой (7 кадров), либо его действию отказано в схеме вслух (16 и
  21). Причина живёт не только в этом тексте, но и в машинной карте — третьим полем
  `tools/corpus/frames.tsv`, — поэтому отчёт прогона печатает по строке на причину, а не одно
  «пока» на всех (03.08.2026, раздел «Вне счёта — по названной причине»).

Куда смотреть:

- **какой номер про что** — раздел «Карта кадров»; машинная копия для счёта —
  `tools/corpus/frames.tsv` (правятся вместе, расходиться им нельзя);
- **устройство метрики** (что считается провалом, как живёт «⚠», приватность эталонов) — раздел
  «Как это меряется»;
- **сами замеры** — «Летопись замеров», в хронологическом порядке, каждый как записан;
- **код:** `core/flow/CorpusMetric.kt` (доля готовности) и `core/flow/TableMetric.kt` (таблицы),
  тесты рядом; прогон — `tools/corpus-run.sh`, счёт таблицы — `tools/table-score.sh`; эталоны —
  `tools/corpus/*.expected.tsv`;
- **сам корпус — вне репозитория** (`C:/Users/User/point-corpus/` на машине владельца).

### Расхождения между разделами — названы, а не сглажены

- **расхождение: кадр 17 в облаке.** Ручная проверка (01.08.2026) записала, что после одного тапа
  «Распознать в облаке» показание извлекается само (`entity.meter = 0208425`); автоматический
  прогон с тапом того же дня записал по кадру 17 «облако ответило, показание не собралось». Оба
  факта оставлены как записаны; какой из них воспроизводится, скажет следующий прогон.
- **расхождение: колонок у кадра 23.** Образец эталона ниже печатает «колонок 8», а карта кадров,
  таблица размеченных кадров и сам файл `tools/corpus/23.expected.tsv` говорят девять — файл прямо
  пишет: «Заготовка называла восемь — пересчитано по шапке кадра». Число в образце — старая
  заготовка, оно оставлено внутри образца как есть.
- **расхождение: «семь кадров из 22 (32%)»** в разделе про таблицы — это счёт первого замера. Карта
  по тем же 22 кадрам даёт шесть (разбор — «Расхождение на один кадр»); семь табличных кадров в
  корпусе действительно семь, но только вместе с кадром 23.

### Что выглядит расхождением, но им не является

- **Знаменатель кадра 23: то 36 сверенных ячеек, то 49.** Ночные прогоны 02.08 сверяли 36 —
  эталон дописан человеком после них и вырос до 49 (26 чисел + 23 названия). Это разные даты, а не
  спор; сравнивать числа через эту границу нельзя.
- **Вне счёта то 14 кадров, то 15.** Между двумя расширениями измеримости в корпус добавился сам
  кадр 23, поэтому 14 + 1 = 15.

## Карта кадров: какой номер про что

Счётчики по категориям (ниже, в замерах) говорят, **сколько** кадров ждут схемы, но не говорят
**каких**: «извлечь таблицу — 7 кадров» — это цифра, по которой нельзя начать работу. Чтобы
узнать, что именно не измерено, приходилось каждый раз пересматривать корпус глазами. Карта
закрывает эту дыру раз и навсегда.

Кадры описаны **обезличенно**: назван вид документа, а не его содержимое — «фото бумажной
накладной», а не её номер, «переписка с реквизитами», а не реквизиты. Правило корпуса («данные
владельца не живут в репозитории») распространяется на карту целиком: карта нужна, чтобы знать,
чего у нас ещё нет, а не чтобы пересказать документ.

Кадров сейчас **23**; в таблицах первых двух замеров фигурируют 22 — кадр `23` (ведомость) снят
позже первых прогонов, и старые числа задним числом не переписываются. Третий замер (ниже) идёт
уже по всем 23.

| кадр | что на снимке | что владелец хочет с этим сделать | категория |
|---|---|---|---|
| 01 | скриншот первого экрана Point, внутри открыто фото техкарты блюда — тот же документ, что на кадре 04 | извлечь таблицу продуктов | таблица |
| 02 | скриншот переписки в мессенджере: реквизиты карты и сумма | перевести деньги | прочее |
| 03 | скриншот переписки: два фото приборов учёта и расчёт суммы к оплате | перевести деньги по показаниям | прочее |
| 04 | фото бумажной технологической карты блюда с таблицей продуктов, бумага под углом | извлечь таблицу | таблица |
| 05 | скриншот входящего сообщения в поддержку (iOS), вопрос клиента | ответить | письмо |
| 06 | скриншот окна учётной программы: таблица договоров поставки | извлечь таблицу | таблица |
| 07 | фото экрана монитора: письмо клиента в почтовом клиенте | ответить | письмо |
| 08 | экран банковского приложения «платёж отправлен» со ссылкой на квитанцию | переслать квитанцию | квитанция |
| 09 | фото водомера, барабанное табло, блик на стекле | передать показание | счётчик |
| 10 | фото бумажной ведомости остатков, снято вверх ногами; печатная сетка + записи от руки | извлечь таблицу | таблица |
| 11 | карточка посылки в приложении службы доставки | отследить отправление | отследить |
| 12 | скриншот карты: карточка отделения службы доставки с кнопкой маршрута | построить маршрут | маршрут |
| 13 | фото бумажной экспресс-накладной службы доставки | отследить отправление | отследить |
| 14 | скриншот карт с построенным автомобильным маршрутом между двумя точками | построить маршрут | маршрут |
| 15 | фото водомера, барабанное табло, тёмный кадр | передать показание | счётчик |
| 16 | скриншот приложения-канбана: карточка со списком покупок и галочками | собрать список в текст | прочее |
| 17 | фото электросчётчика, барабанное табло, запылённое стекло | передать показание | счётчик |
| 18 | фото бумажной сводной таблицы к счёту, снято вверх ногами, лист смят по сгибу | извлечь таблицу | таблица |
| 19 | фото страницы служебного документа: рукописная таблица на защитной сетке бланка | извлечь таблицу | таблица |
| 20 | банковская квитанция: лист с полями «отправитель / получатель / сумма», печать | переслать квитанцию | квитанция |
| 21 | фото экрана монитора: программа обмена документами, дерево еженедельных отчётов | найти и забрать нужный отчёт | прочее |
| 22 | скриншот карт: точка, найденная по координатам | построить маршрут | маршрут |
| 23 | фото продовольственной ведомости (35 строк, 9 колонок) с правками от руки поверх печати | извлечь таблицу | таблица |

### Что видно из карты и не видно из счётчиков

- **Ни одного «чистого скана» среди таблиц.** Семь табличных кадров — это бумага под углом (04),
  кадр вверх ногами (10, 18), рукопись поверх печати (10, 19, 23), окно чужой программы (06),
  скриншот приложения поверх фотографии (01). Метрика таблиц меряет не распознавание таблицы, а
  распознавание **такой** таблицы.
- **Больше половины корпуса — снимки экрана, а не бумага** (12 из 23). Это не «лёгкие кадры»:
  два из них (07, 21) — фото **чужого** монитора, с бликом, муаром и наклоном, то есть худший
  вход из всех, хотя текст на них рождён компьютером.
- **Кадр бывает гибридным, категория — нет.** На 03 внутри переписки лежат два прибора учёта, на
  02 — номер отправления (его Point и нашёл на первом замере). Категория кадра — то, что человек
  хочет сделать, а не всё, что на снимке видно; иначе счётчики начнут расти от каждой находки.
- **«Прочее» — это не мусор, а три ненаписанных действия**: перевести деньги по реквизитам из
  переписки (02, 03) и забрать список/файл из чужого приложения (16, 21). Два кадра из четырёх —
  про деньги, и это самое дорогое «прочее» в корпусе. *Написано (02.08.2026):* деньги получили
  схему `pay-by-requisites`, а 16 и 21 — названный отказ (ниже, «Расширение измеримости: деньги,
  квитанция и ответ»). *Проверено глазами по самим снимкам (03.08.2026):* отказ подтверждён, и
  доводы стали тяжелее — «Вне счёта — по названной причине».

### Расхождение на один кадр — названо, а не спрятано

Первый замер посчитал «извлечь таблицу — 7 кадров» по корпусу из 22. Карта по тем же 22 кадрам
даёт **шесть**: 01, 04, 06, 10, 18, 19. Седьмым в том счёте, судя по всему, шёл один из двух
списков без сетки — карточка списка покупок (16) или дерево файлов отчётов на экране (21). По
существу это не таблицы, и здесь они названы «прочим».

Ни старый счёт не переписан (его сказал замер, а не автор правки), ни карта не подогнана под
цифру. С кадром 23 табличных кадров в корпусе всё равно семь — но это уже другие семь, и теперь
они названы поимённо.

## Как это меряется

Здесь собрано устройство обеих метрик: что считается, что считается провалом, как живёт знак
неуверенности и почему часть эталонов не может лежать в репозитории. Числа — не здесь, а в
летописи.

### Метрика: доля кадров, где действие выполнено без правок

**Доля кадров, где действие выполнено без правок человеком.** Бинарно по кадру: у кадра есть
ожидаемое действие, и оно либо готово по своим критическим полям (`ActionSchema.readiness`,
#260), либо нет. Не field-F1 и не «заполнено 6 из 9» — такое число подделывается количеством
заполненных полей и растёт, когда система заполняет пустоты.

Кадры, чьё действие не имеет схемы, в знаменатель не идут, но **называются поимённо и с
причиной** (`CorpusScore.unscored`): молчаливое сужение корпуса до удобных кадров — тот же грех,
что красивая ложь про «6 из 9».

Код метрики — `core/flow/CorpusMetric.kt`, тесты — `CorpusMetricTest`.

### Вне счёта — по названной причине, а не под общим «пока» (03.08.2026)

Причин стоять вне знаменателя ровно три, и это **разные новости** (`OutOfCount`):

| причина | слово в карте кадров | что значит человеку |
|---|---|---|
| таблица | `таблица` | кадр измерен, но другим числом: по самой таблице, которую он получил (`scoreTable`) |
| отказ | `отказ` | схемы не будет — решение записано (кадры 16 и 21) |
| ждёт схемы | `ждёт схемы` | схемы ещё нет, это законное «пока» (сейчас таких кадров нет) |

До этого все девять печатались одной строкой — «пока не проверяем, не описано что считать
успехом», — и она врала дважды: таблицы уже измерены, а слово «пока» обещало работу по кадрам, по
которым её решили не делать. Теперь причина едет третьим полем `tools/corpus/frames.tsv`, отчёт
печатает по строке на причину, а два сторожа держат карту честной: **вне счёта без причины не
стоит никто** (так же ловится опечатка в id схемы — кадр иначе тихо уменьшил бы знаменатель) и
**причина не остаётся на кадре, у которого появилась схема**. Кадр, всё же выпавший без причины,
отчёт называет прямо: это дыра в карте, а не свойство кадра.

Почему схемы не будет двум кадрам — разбор в `DECISIONS.md` («Последние два кадра корпуса схемы не
получают»); коротко: у списка (16) нет **одного** значения, которое либо прочитано, либо нет —
тринадцать пунктов из четырнадцати схема объявила бы готовностью, а человек получил бы список без
хлеба (и сам снимок прокручен, верхний пункт срезан экраном); у дерева отчётов (21) на странице
нет самого предмета действия — какой отчёт нужен, знает человек, а файл лежит на чужом компьютере,
куда от фотографии монитора пути нет.

### Где живёт корпус

**Не в репозитории.** 22 кадра владельца — это его карты, платежи, переписка и военная
ведомость; дословный OCR-дамп такого кадра — те же данные. Корпус лежит вне репо
(`C:/Users/User/point-corpus/` на машине владельца), разметка кадров и результаты прогонов —
в комментариях issue #262.

В репозитории живут: харнесс прогона (`tools/corpus-run.sh`), код метрики и её тесты, а также
дословные фикстуры **обезличенных** кадров, если такие появятся (правило «фикстура — дословный
вывод устройства, не сочинённый текст» остаётся в силе).

### Таблицы меряются по результату действия, а не по готовности полей (#262)

Семь кадров из 22 (32%) — это «извлечь таблицу», и до сих пор они не мерились **ничем**: лежали в
`unscored` рядом с теми, кому просто ещё не завели схему.

**Схемы готовности у таблицы не будет.** Честного факта «в документе есть таблица» не существует:
любой признак («много чисел в строке», «модель вернула сетку») — не свойство документа, а наша
догадка о нём. Схема поверх догадки накрутила бы метрику **пустой готовностью**: кадр объявлялся бы
готовым ровно потому, что мы разрешили себе так думать. Поэтому таблица меряется по **результату
действия** — по тому самому `.xlsx`, который человек открыл.

Из этого следует и второе: число таблиц **не вливается** в долю «действие без правок». «В Excel» —
платное сетевое действие за явным тапом, а та доля меряет, что Point понял **сам**. Это два разных
обещания человеку, и среднее между ними не значит ничего. Кадры таблиц остаются в `unscored`
корпусной метрики — но «нет схемы готовности» больше не равно «не измерено».

### Эталон: что человек размечает один раз

Один текстовый файл на кадр, ~5 минут. Заготовка кадра 23 (ведомость владельца) —
`tools/corpus/23.expected.tsv`, разбор — `parseTableExpectation` в `core/flow/TableMetric.kt`:

```
строк 35            # строк ДАННЫХ в документе, без шапки — обязательно
колонок 8           # колонок в документе — обязательно
ключ 1              # номер колонки-ключа, счёт от 1 (на ведомости это артикул)
шапка да            # есть ли строка заголовков
--
11004	Гречка		120
11006
```

После `--` — по строке на каждую строку документа, которую **есть чем назвать**: поле в
колонке-ключе — ключ, остальные непустые поля — значения, которые человек сверил глазами, пустые —
«не сверял». Строки без артикула назвать нечем; они входят только в число `строк`, и метрика знает,
что столько неопознанных строк в файле законны.

`строк` и `колонок` не имеют значений по умолчанию сознательно. Взять их из числа названных строк
было бы удобно и было бы ложью: эталон, назвавший 27 строк из 35, объявил бы документ 27-строчным —
и восемь потерянных строк исчезли бы из метрики вместе с провалом.

*Число `колонок 8` в образце выше — старая заготовка кадра 23; сам файл эталона пересчитан по шапке
и говорит девять (см. «Расхождения между разделами»).*

### Какие кадры размечены (02.08.2026)

Табличных кадров семь. Эталон есть у **пяти**, и в каждом сказано, как именно человек сопоставлял
значения со строками — метрика без этого судит по числам неизвестного происхождения.

Последняя колонка — **знаменатель метрики на этом кадре**: сколько ячеек `scoreTable` сверяет
(`TableScore.checkedCells`), а не сколько строк названо.

| кадр | что за таблица | строк × колонок | ключ | сверено значений |
|---|---|---|---|---|
| 01 | миниатюра техкарты внутри снимка экрана Point | 12 × 7 | № з/п | 25 |
| 04 | та же техкарта, снята целиком, лист под наклоном | 12 × 7 | № з/п | 25 |
| 06 | окно учётной программы, список договоров | 16 × 7 | номер договора | 24 (год и даты) |
| 18 | сводная таблица к счёту, лист вверх ногами | 2 × 7 | артикул | 10 |
| 23 | продовольственная ведомость, рука поверх печати | 35 × 9 | артикул | 49 (26 чисел + 23 названия) |

Разбор всех файлов каталога держит `TableMetricTest`: опечатка в эталоне иначе всплывает только
после прогона на устройстве — через минуты ожидания и один потраченный кадр. Второй тест держит
сами числа — и строки, и значения: пропавшая при правке табуляция убирает одно сверенное значение,
строка остаётся «со значениями», и знаменатель тихо становится меньше напечатанного здесь.

**Двух кадров в таблице нет, и это решение, а не забывчивость** — причины названы ниже, в
«Приватности эталона»: кадр 10 (край кадра срезал колонку с названиями строк) и кадр 19 (таблица
целиком — персональные данные).

Тесты читают эталоны файлом, мимо classpath, поэтому каталог `tools/corpus` объявлен **входом**
задачи `:core:flow:test` (`core/flow/build.gradle.kts`). Без этого правка эталона не меняет для
Gradle ничего: задача остаётся UP-TO-DATE, при `org.gradle.caching=true` берётся FROM-CACHE, и
`./gradlew test` печатает BUILD SUCCESSFUL, ни разу не запустив проверку — сторож есть, а не
сработал.

### Что считает метрика таблиц

`scoreTable(эталон, таблица)` — чистая функция в `:core:flow` (рядом с `CorpusMetric.kt`), тесты —
`TableMetricTest`. Строки узнают друг друга **по колонке-ключу**, а не по номеру: колонка-ключ
файла ищется как та, где нашлось больше всего эталонных ключей, и тем же сдвигом переносятся номера
колонок сверяемых ячеек — пустая колонка бланка слева не должна объявлять расхождением всю таблицу.

| число | что значит |
|---|---|
| найдено / потеряно строк | сколько названных строк документа нашлось в файле, сколько нет (поимённо) |
| лишних строк | неопознанных строк **больше**, чем эталон мог не назвать. Так ловится и подмена: три строки потеряны, три придуманы, длина файла та же |
| доля ячеек | сверенных ячеек, совпавших с эталоном, из всех сверенных |
| помечено ⚠ | расхождений, о которых файл предупредил: цена проверки, не ложь |
| **прошло молча** | расхождений без единого знака — **главное число** |
| доля ⚠ по файлу | сколько ячеек файла помечено из всех: отдельное число, потому что стена предупреждений — тоже провал |

Ячейки потерянной строки считаются молчаливыми расхождениями наравне с испорченными: для человека
это одно и то же — значения нет, и ничто не сказало, что его нет.

### Что считается провалом

Провал — не «доля ниже порога», а **названная причина** (`TableScore.failures`). Причин пять:

1. **Молчаливое расхождение** (`SILENT_CELLS`) — значение разошлось с эталоном, и ничто в файле об
   этом не сказало. Любое число больше нуля — провал, потому что это ровно та ложь, которую человек
   не может поймать: он поверит цифре.
2. **Потерянная строка** (`LOST_ROWS`) — строка документа в файл не попала, и файл об этом молчит.
3. **Лишняя строка** (`EXTRA_ROWS`) — в файле есть строки, которых в документе нет.
4. **Стена предупреждений** (`WARNING_WALL`) — ⚠ стоит на трети ячеек и больше. Формально это
   честность, по существу — невыполненное действие: человек получил не таблицу, а задание
   перепроверить её целиком. Прецедент прямой: живой прогон кадра 23 отдал 54 строки вместо ~35 и
   пометил спорными 387 ячеек из ~430 (90%) при нуле молчаливых расхождений. Порог `1/3` —
   суждение, а не измерение, и назван вслух именно поэтому (`WARNING_WALL_SHARE`).
5. **Не та ширина** (`WRONG_SHAPE`) — колонок в файле не столько, сколько в документе.

Исходов при этом **три**, а не два. Третий — **«нечем судить»** (`TableScore.unjudged`): причин
провала нет, но и сверенных ячеек нет — эталон назвал строки и не назвал ни одного значения. Ровно
так выглядит заготовка кадра 23, пока владелец не допишет её глазами: файл, где артикулы прочитаны
и ширина сошлась, а содержимое пусто или выдумано, не даёт ни потерь, ни молчаливых расхождений.
«Сдано» здесь было бы статусом, выданным за отсутствие проверки, — той самой красивой видимостью
вместо факта. Поэтому отчёт говорит словами: сошлись только строки и ширина.

### Годность файла: вторая величина таблицы (04.08.2026, #493)

Всё, что описано выше, отвечает на вопрос **«не соврал ли файл»**. Владелец 04.08.2026 получил
таблицу из ведомости и сказал про неё другое слово — «бедовая», — и ни одно из прежних чисел этого
не поймало: файл мог не разойтись с эталоном ни разу и всё равно быть непригодным. Поэтому рядом
встала вторая величина: **можно ли открыть файл и работать**.

Главное её число — **доля листа, которую человек может взять как есть**. Ячейка годится, если она
стоит в документе (а не в части «Непрочитанное»), не помечена ⚠ и не содержит символьного шума.
Знаменатель — все непустые ячейки файла, включая непрочитанное: человек получил лист целиком.

Причин негодности четыре, и каждая называется словом:

| причина | когда | что это значит человеку |
|---|---|---|
| **пусто** | ни одной непустой ячейки | файла как результата нет |
| **свалка** | в «Непрочитанном» больше четверти листа | не таблица, а её обломки |
| **шум** | символьный шум в каждой десятой ячейке и чаще | значения приходится перенабирать с фотографии |
| **стена пометок** | ⚠ стоит на трети ячеек и больше | не таблица, а задание проверить её целиком |

Два порога (четверть и десятая) — **суждение, а не измерение**, и названы вслух именно поэтому;
третий взят тот же, что у стены предупреждений выше, чтобы два числа не разошлись молча.

**Что такое символьный шум.** Правило не судит по «странным буквам» — ошибку чтения ловит эталон.
Здесь ловится то, чего в наборанном человеком значении не бывает **структурно**: знаки `_ | \ ^ { } < >`,
непарная скобка или кавычка, запятая/восклицательный знак/апостроф в конце значения, латиница
и кириллица внутри одного слова. Точка на конце шумом не считается — ею кончаются сокращения и
номера по порядку.

**Ложная тревога измерена, а не обещана.** Правило прогнано по всем значениям, которые владелец
переписал в эталоны корпуса своей рукой (пять файлов, без малого две сотни значений): ни одного
срабатывания.
Держит это тест (`TableUsabilityTest`), а не обещание в тексте: правило, кричащее на человеческий
текст, кричало бы и на хорошо прочитанный файл.

**Годность и правда — разные обещания, и мерить надо оба.** Прогон 04.08.2026 дал прямое
доказательство: файл кадра 01 годен на 75% и при этом **выдуман целиком** — 25 сверенных ячеек из
25 разошлись с документом, и ни одной пометки. Одно число без другого хвалит либо честную кашу,
либо красивую выдумку.

Код — `core/flow/TableUsability.kt`, тесты — `TableUsabilityTest`; считается тем же харнессом
(`tools/table-score.sh`), вторым блоком отчёта, по тому же файлу, что человек открыл. Эталон ей не
нужен — годность видно из самого листа, поэтому она меряется и там, где размеченного эталона нет.

### Чего метрика НЕ ловит (сказано вслух, чтобы число не выглядело больше, чем оно есть)

- **Сверяется выборка, а не таблица.** Ошибка в ячейке, которую человек не размечал, пройдёт мимо
  числа. Доля совпавших ячеек — доля по **сверенному**, и знаменатель печатается рядом с ней.
- **Строки без ключа не названы** — их потерю поймает только счёт лишних/недостающих строк.
- **Пропавшая колонка в середине** сдвинет всё правее себя и покажется расхождением ячеек: провал
  честный, но причина будет названа не та. Ширина файла печатается рядом именно за этим.
- **Пустые строки, выброшенные Excel'ом при пересохранении**, не восстанавливаются: харнесс читает
  строки в том порядке, в каком они лежат в `sheet1.xml`.

### «⚠» в файле живёт стилем, а не текстом

Ловушка, на которой метрика соврала бы в главном своём числе. `OoxmlSpreadsheetWriter` снимает
маркер (`styleCell`) и превращает неуверенность в **заливку ячейки**: человек видит оранжевый фон,
а в тексте `<t>` никакого «⚠» нет. Счётчик, читающий только текст, объявил бы **молчаливым каждое
честное предупреждение** — то есть промахнулся бы в худшую сторону ровно там, где мерит правду.

Поэтому харнесс читает ещё и `xl/styles.xml` и переводит стиль обратно в маркер: `s="N"` →
`cellXfs[N].fillId` → цвет заливки; совпал с `FLAG_FILL` (`tools/table-score.sh`) — «⚠»
возвращается в текст, и дальше его читает та же `styleCell`, что и приложение. Цвет продублирован в
харнессе не молча: `OoxmlSpreadsheetWriterTest` держит его контрактом и падает, если палитра уедет.
Если `styles.xml` в файле нет или заливки в нём не нашлось — это пишется в отчёт словами (перед
числами, которым после этого верить нельзя), а не превращается в тихий ноль предупреждений.

Жёлтая заливка правки («~~53~~ 40») предупреждением **не** считается: она говорит «я увидел
зачёркнутое», а не «я не уверен». Ошибка в такой ячейке молчалива — человек прочтёт число как
окончательное.

### Приватность эталона

Заготовка кадра 23 лежит в репозитории, потому что в ней только артикулы бланка и числа документа —
ни имён, ни сумм, ни фотографии. Как только эталон кадра начинает содержать значения, которые
владелец считает своими (платежи, переписка, персональные данные), он живёт **вне репозитория**,
рядом с самим корпусом, — правило корпуса на эталоны распространяется целиком.

Правило это уже стоило кадров, и они названы поимённо, а не молча пропущены.

- **Кадр 06 размечен наполовину — сознательно.** Это список договоров поставки: колонки
  «Постачальник» и «Ціна» называют контрагентов и цену, и в эталон они не попали. Сверены только
  обезличенные ячейки — год, дата договора, дата начала действия. Номер договора остался ключом:
  без ключа строки не узнают друг друга вовсе, а сам по себе он не называет ни имени, ни суммы.
  Цена решения честная и видимая: доля совпавших ячеек на этом кадре меряет чтение **дат**, а не
  всей таблицы, и знаменатель напечатан рядом с числом.
- **Кадр 19 эталона не имеет и не получит.** Это страница служебного документа: рукописная
  таблица прохождения службы — должности, даты, номера приказов. Здесь нечего обезличивать:
  персональные данные — не отдельная колонка, а сама таблица. Кадр остаётся в корпусе (по нему
  можно прогнать действие глазами), но числа по нему в репозитории не живут.
- **Кадр 10 эталона не имеет по другой причине — читать нечего.** Лист снят вверх ногами, и край
  кадра срезал **колонку с названиями строк**: от них остались обрывки («…НИ ДЛЯ ВОДИ»,
  «…АРЧОВА»). Колонки-ключа в кадре нет вовсе — ни номера по порядку, ни кода, — а значит ни одну
  строку нельзя назвать так, чтобы метрика узнала её в файле. Придумать ключ означало бы сверять
  выдуманное; эталон из одних `строк`/`колонок` метрика честно объявит «нечем судить»
  (`TableScore.unjudged`), и пользы от него ноль. Плюс к тому подписи под таблицей — фамилии
  должностных лиц, то есть и приватность здесь не пустая.

### Как прогнать

Весь корпус — кадр за кадром, дословно с устройства:

```bash
bash tools/corpus-run.sh out /path/to/corpus/*.jpg
```

На каждый кадр харнесс расшаривает его в Point, ждёт строку `OCR done` в логе (на софтверном
эмуляторе распознавание идёт десятками секунд — «на глаз» ждать нельзя) и забирает **дословно**:
`<кадр>.txt` — текст из scratch, `<кадр>.atoms.tsv` — слой атомов (слово+bbox+уверенность),
`<кадр>.flow.json` — журнал метаданных объекта (вход метрики), `<кадр>.png` — снимок экрана.
Ничего не выдумывает: в отчёт идёт только то, что вернуло устройство.

Рецепты шаринга различаются на эмуляторе (API 36) и на телефоне владельца — см. #257.

Таблица — тот же харнесс с тапом по действию и эталоном:

```bash
# весь путь: кадр → устройство → тап «В Excel» → .xlsx → числа
bash tools/corpus-run.sh --tap "В Excel" --table tools/corpus/23.expected.tsv out ~/point-corpus/23.jpg

# только счёт, по уже снятому файлу
bash tools/table-score.sh out/23.xlsx tools/corpus/23.expected.tsv
```

`.xlsx` — это zip с `xl/worksheets/sheet1.xml`, поэтому библиотек не нужно: `unzip` + `awk`
(`tools/xlsx-to-tsv.awk`) достают ячейки дословно в `.tsv` рядом с файлом — улику, по которой счёт
можно проверить. **Считает при этом Kotlin**, а не шелл: у метрики одна реализация
(`:core:flow:scoreTable`), и она же под тестами; вторая копия правил на awk разошлась бы с первой
молча — ровно та болезнь, от которой метрика и лечит.

Цена такого выбора — старт Gradle на каждый счёт; рядом с минутами ожидания устройства (одно
«В Excel» на кадре 23 заняло ~2.5 минуты) это приемлемо, а расхождение метрики с её же тестами —
нет.

Если `.xlsx` на устройстве нет или счёт не поднялся — это пишется в отчёт словами. Тихий ноль
опаснее честного отказа. Таблица прошлого кадра стирается **до** прогона — и из scratch на
устройстве, и из каталога вывода: иначе «самый свежий .xlsx» оказался бы чужим, и метрика посчитала
бы предыдущий кадр, ничем себя не выдав.

После тапа харнесс **ждёт сам файл**, а не фиксированные секунды (`TABLE_WAIT`, по умолчанию 300 с;
живое «В Excel» на кадре 23 заняло ~2.5 минуты). Слепое ожидание короче работы объявило бы «файла
нет» там, где действие ещё идёт, — то есть соврало бы отказом, и это та же подмена факта, от
которой лечит сама метрика. Сколько ждали, пишется в отчёт: «не успели» и «не сделал» — разные
ответы.

Три вещи, которым харнесс научился 04.08.2026, — все про то же самое: не путать «не сделал» с
«мы не так спросили».

- **Согласие на облако** (#468). Сетевое действие спрашивает человека, прежде чем объект уедет
  наружу. Харнесс, написанный до этого вопроса, тапал «В Excel» и ждал файл, которого не будет
  никогда, а в отчёт писал «действие до файла не дошло» — то есть выдавал за отказ продукта свой
  собственный. Теперь согласие подтверждается **и пишется строкой в отчёт**: замер без этой строки
  выглядел бы так, будто объект уехал в облако сам.
- **Конец действия виден по экрану.** Отказ («на странице не нашлось ничего, что можно положить в
  таблицу») файла не создаёт, и ожидание досиживало предел до конца: полчаса на кадр ради того же
  ответа, что у не успевшего. Теперь харнесс спрашивает экран, идёт ли ещё действие.
- **Предел ожидания чтения** больше не зашит числом (`OCR_WAIT`, по умолчанию те же 200 с):
  скорость машины к качеству чтения отношения не имеет, а «не успел» и «не смог» — разные ответы,
  и в отчёте теперь видно, сколько ждали и каков был предел.

## Летопись замеров

Записи идут в хронологическом порядке и **не переписываются**: число говорит харнесс, а не автор
правки. Замер судит только тот код, который в нём ехал; если причина позже починена — это видно в
следующей записи, а не в исправленной прошлой. Текущее состояние — в сводке наверху.

### Первый замер (01.08.2026, эмулятор, 22 кадра)

Слой атомов сняли **22/22** кадра (36–385 слов), текст — 16/22 (шесть закрыл гейт мусора; слой
при этом сохранён — улика не уничтожается гейтом показа).

Измеримых кадров — 2 (оба «отследить отправление»): **1 из 2 готов**.
- ✅ `11` — карточка Новой Пошты: трек прочитан правилом, действие готово без облака.
- ❌ `13` — фото бумажной накладной, повёрнутое на 90°: трек не прочитан (ориентация).
- Бонусом: `02` — скрин переписки со вложенным треком — трек найден (гибридный вход живёт),
  хотя ожидаемое действие кадра другое (перевести деньги).

Остальные 20 кадров — `unscored`: их действия (извлечь таблицу — 7 кадров, показание счётчика —
3, маршрут — 3, переслать квитанцию — 2, ответить на письмо — 2, …) ещё не имеют схем. Это и
есть карта следующей работы, а не повод считать метрику по удобным кадрам.

### Расширение измеримости: маршрут и счётчик (01.08.2026)

Схемы двух самых частых после таблицы действий заведены — **вместе с фактами, которых у объекта
не было** (схема без факта лжёт в обе стороны):

| Действие | Схема | Критическое поле | Откуда факт |
|---|---|---|---|
| Построить маршрут | `route` | адрес **или** координаты | `entity.address` (энричер сущностей) + новый `entity.geo` (правило `geoFacts` + ключ контракта `GEO`) |
| Передать показание счётчика | `meter-reading` | само показание | новый `entity.meter` + `entity.meter.unit` (правило `meterFacts` + ключ контракта `METER`) |

Измеримыми становятся **6 кадров** (`09`, `15`, `17` — счётчик; `12`, `14`, `22` — маршрут):
знаменатель метрики растёт с 2 до 8, `unscored` — с 20 до 14. Само число готовых даст только
следующий прогон харнесса: правило видит показание рядом с единицей учёта, а на голом табло
водомера единицы может не быть — и тогда честный ответ «не готово», а не исчезнувший кадр.

Ограничения названы вслух, а не спрятаны:
- **гейт мусора** (`looksLikeOcrGarbage`) закрывает текст на кадрах с двумя словами — а фото
  счётчика именно такое; слой атомов при этом сохраняется, значит путь к показанию есть
  («Понять» по слою), но офлайновое правило на таком кадре не сработает;
- **по координатам Point ещё не поедет**: `MapRealizer` ищет адрес on-device и на паре чисел
  вернёт «Адрес не найден». Факт заведён раньше действия сознательно — метрика меряет
  прочитанное;
- **объём потребления и показание правило не различает**: «спожито за місяць 250 кВт·ч» той же
  формы, что показание табло. Улика одна, поэтому человек видит «возможно», а не «факт»;
- **карточку маршрута зовут координаты, а не адрес.** Адрес критичен для готовности, но якорем
  не сделан: он есть на чеке, счёте, в подписи письма и на карточке посылки, и якорь на нём
  звал бы «Построить маршрут» на каждом из них (ошибка, которую чинил #260). На измеримость
  это не влияет — метрика считает `readiness()`, где якоря нет, — но кадры `12` и `14` до
  появления собственного признака «места» карточку не покажут.

### Второй замер (01.08.2026, эмулятор, после автоориентации и двух новых схем)

Схемы «маршрут» и «показание счётчика» подняли **измеримость** с 2 кадров до 8. Готовность
при этом — **2 из 8**, и это самый полезный результат за оба замера: он называет, где продукт
не дотягивает, вместо того чтобы льстить.

| кадр | действие | итог | почему |
|---|---|---|---|
| 11 | отследить отправление | ✅ | трек прочитан правилом, без облака |
| 22 | построить маршрут | ✅ | координаты прочитаны правилом |
| 13 | отследить отправление | ❌ | номер накладной 13-значный, правило ищет ровно 14 |
| 12 | построить маршрут | ❌ | адрес отделения на карте не собрался в текст |
| 14 | построить маршрут | ❌ | населённый пункт есть в тексте, но как адрес не опознан |
| 09 | показание счётчика | ❌ | **распознавание не читает фото прибора вовсе** |
| 15 | показание счётчика | ❌ | то же: слой атомов — символьный шум |
| 17 | показание счётчика | ❌ | то же |

#### Что сказал замер

**Счётчики (0 из 3 офлайн) — потолок движка на устройстве, но не тупик.** Дословные атомы с устройства на этих
кадрах выглядят так: `И. | + | м | 4 | с | . | . | ‘i`. Tesseract не читает семисегментные и
барабанные табло под бликом; никакая схема поверх этого шума показание не соберёт. Путь к
показанию есть только через зрячую модель («Понять») либо через предобработку кадра (кроп табло
и увеличение) — это отдельная работа, и её цена теперь измерена, а не угадана.

**Карты (0 из 2) — адрес не опознаётся в тексте карты.** На кадре 12 в распознанном есть
«Нова Пошта», «ддлення N21» — то есть отделение читается, но как **адрес** не размечается;
на кадре 14 есть «Новониколаевка, Запорожская о…». Здесь потолок не в OCR, а в извлечении:
адресом считается то, что нашёл извлекатель сущностей, а он на обрывках карты молчит.

**Накладная (13) — правило формы уже, чем реальность:** номер из 13 цифр не считается треком,
потому что правило требует ровно 14 (форма Новой Почты). Это ровно тот случай, ради которого
у идентификатора есть второй источник — модель; офлайн-путь здесь бессилен по построению.

#### Вывод для работы

Улучшать нужно не схемы, а два конкретных места: **чтение приборов** (кроп + увеличение либо
зрячая модель) и **разметку адреса** на скупом тексте карт. Обе задачи теперь имеют цену в
кадрах корпуса, а не в ощущениях.

#### Уточнение к замеру: счётчики читаются, но не офлайн (проверено вручную)

Автоматический прогон снимает журнал сразу после распознавания — то есть меряет **только
офлайновый путь**. Ручная проверка кадра 17 (электросчётчик) на том же билде показала другое:
после одного тапа «Распознать в облаке» показание извлекается правилом само —

```
entity.meter = 0208425      entity.meter.unit = kW·h
entity.meter.src = ocr      entity.meter.ev = semantic
```

Значит вывод точнее прежнего: **на устройстве приборы не читаются, в облаке читаются**, и путь
к показанию стоит один явный тап — ровно так, как задумано в продукте (платное и сетевое живёт
за тапом, а не в фоне). С правкой «„Понять“ читает пиксели» такой же путь открылся и там, где
текста нет вовсе.

Отсюда две честные величины вместо одной: доля **без облака** (сейчас 2 из 8) и доля **после
одного тапа в облако** — её измерит следующая версия харнесса, которая умеет тапнуть действие.
Смешивать их в одно число нельзя: это разные обещания человеку.

### Вторая величина измерена (01.08.2026): что даёт один тап в облако

Харнесс научился тапать действие по имени кнопки (`--tap "Распознать в облаке"`), и вторая
величина перестала быть предположением. Прогон по кадрам, которые офлайн не осилил:

| кадр | действие | после одного тапа | значение |
|---|---|---|---|
| 09 | показание счётчика | ✅ | `00001154` — то самое «154 м³» с ведущими нулями барабана |
| 15 | показание счётчика | ✅ | `007145` |
| 17 | показание счётчика | ❌ | облако ответило, показание не собралось |
| 12 | маршрут | ❌ | адреса нет и в облачном чтении карты |
| 13 | отследить | ❌ | номер 13-значный, правило требует 14 |

**Итог по двум величинам.** Без сети — 2 из 8. После одного явного тапа приборы, которые
устройство не читает вовсе, читаются в двух случаях из трёх. То есть узкое место счётчиков —
не «Point не умеет», а «на устройстве не умеет», и продукт честно предлагает путь за тап.

**Значение приходит дословно, с ведущими нулями барабана** (`00001154` вместо «1154»). Обрезать
их молча нельзя: сколько разрядов на табло значащих, знает поставщик услуги, а не мы. Отдельная
задача **сделана** (`meterWithoutDrumZeros` → `FieldReading.hint`): значение остаётся дословным
везде — в метаданных, в журнале, на экране, — а карточка готовности говорит рядом «показание —
со страницы „00001154“, обычно передают „1154“». Оба числа видно, и видно, какое из них с
прибора; метрики это не касается вовсе — готовность считается по прочитанному факту, а не по
подсказке.

Границу разрядов замер при этом ставит впритык: `00001154` — ровно 8 цифр, потолок правила
(`METER_MAX_DIGITS`), и девятипозиционное табло оно не прочитает вовсе. Живого такого кадра в
корпусе нет — двигать границу стоит с ним в руках, а не «на всякий случай».

**Помеха, которую пришлось убрать в инструменте:** софтверный эмулятор вешает поверх приложения
диалог «System UI isn't responding», и первый прогон с тапом молча мерил пустоту («кнопка не
найдена» на всех кадрах). Теперь харнесс убирает помеху и пишет об этом в отчёт — тихий ноль
опаснее честного отказа.

### Что замер уже изменил в коде (не в числе)

Таблицы выше — записи прогонов, и переписывать их задним числом нельзя: число говорит харнесс, а
не автор правки. А вот причины, которые они назвали, чинятся поимённо:

| причина из таблицы | что сделано | чем закрыто |
|---|---|---|
| кадр 13 — «номер накладной 13-значный, правило ищет ровно 14» | правило знает четыре формы (14 цифр, 13–14 через косую, 13 цифр рядом со словом-маркером, S10 по контрольной цифре) | `core/flow/Identifiers.kt`, запись в `DECISIONS.md` (#262) |
| кадр 13, вторая половина той же причины — подпись стоит **над** номером, а окно соседства считало только свою строку | окно меряется в токенах, а не в строках: на соседнюю непустую строку оно переходит, когда на своей стороне значения токенов нет вовсе | `core/flow/Identifiers.kt` (`markerNear`), `IdentifiersTest`, `RealOcrTest` |
| кадр 12 — «адрес отделения на скриншоте карты не собрался в текст» | офлайновое правило формы читает адрес там, где извлекатель сущностей молчит: `<пункт>, <улица>, <дом>` и `<пункт>, <адм. единица>` | `core/flow/PostalAddress.kt`, `PostalAddressTest` (дословная фикстура кадра 12 — `ocr/parcel_3.txt`) |
| кадр 14 — «населённый пункт есть в тексте, но как адрес не опознан» | та же форма `<пункт>, <адм. единица>` читается правилом — **но на самом кадре 14 строка обрезана экраном** (`Новониколаевка, Запорожская о...`), и офлайн этого мало; путь кадра — «Понять» по пикселям (ключ `PLACE`) | `core/flow/PostalAddress.kt` + названная граница там же и в `DECISIONS.md` |

Следующий прогон харнесса скажет, сдвинулись ли обе величины; до него строки кадров 12–14
остаются такими, какими их измерили.

**Чего в репозитории нет и почему это названо.** Дословных фикстур движка для кадров 13 и 14 нет
— они снимаются харнессом с устройства. Правка кадра 13 проверена на **вёрстке кадра, снятой
глазами с фотографии** (подпись строкой выше номера), и на дословных фикстурах — в обратную
сторону: соседняя строка `neg_viber` и отсутствие новых номеров в ведомости кадра 23. Правка
кадров 12 и 14 проверена на дословной фикстуре кадра 12 полностью, форма кадра 14 — только
формой, потому что дословное чтение самого кадра обрезано экраном.

### Подготовка кадра прибора: что она даёт на трёх кадрах счётчиков (#262)

> *Пометка, дописанная 04.08.2026:* механизма, который здесь измерен, в продукте больше нет —
> действие «Прочитать показание» и вся его геометрия удалены (#396, разбор в `DECISIONS.md`).
> Числа записи не трогаются: они верны про тот код и тот день. Знаменатель метрики удаление не
> изменило — почему, сказано в сводке наверху.

Замер выше назвал причину нуля из трёх: движок на устройстве не читает фото прибора **вовсе**.
Ответ — не другой движок, а другой вход: найти табло, вырезать, довернуть, увеличить, привести к
контрасту и читать **только цифры** (`MeterReader`, решение — в `DECISIONS.md`).

Поиск табло прогнан по кадрам 09/15/17 **вне репозитория** (сам корпус там же), результат снят
глазами по вырезанным кускам. Записывается как есть — включая промах:

| кадр | прибор | верхнее место | что в вырезанном куске |
|---|---|---|---|
| 17 | электро, СО-ЭА09 | ✅ | **весь барабан, выпрямленный**: `0 2 0 8 4 2 5` во всю полосу — ровно тот вход, которого движку не хватало |
| 15 | вода, GIDROTEK | ⚠️ | барабан `0 0 0 7 1 4 5` **внутри куска**, но вместе с половиной циферблата: цифры есть, полосой не выделены |
| 09 | вода, ECOSTAR | ❌ | промах: барабан стоит почти вертикально, фон — гравий; ни одно из трёх мест его не содержит |

Что из этого следует и чего не следует:

- **Число «сколько показаний прочиталось» здесь не называется.** Его говорит устройство, а не
  автор правки: на эмуляторе и телефоне работает настоящий Tesseract с цифровым словарём, и
  прогон харнесса — единственный источник этого числа. До него честная формулировка одна:
  вход для движка на одном кадре из трёх стал таким, каким он должен быть, на втором — лучше
  прежнего, на третьем не изменился ничем.
- **Промах кадра 09 — не аварийный случай, а ожидаемый.** Поиск отдаёт до трёх мест, ни одно из
  них барабана не содержит; облако этот кадр уже читало (`00001154`), и путь туда остаётся.
- **Кадр 15 — граница метода.** Место найдено правильно, но рамка шире табло: высота знака
  оценена по пятну крупнее цифры, и поле вокруг выросло вместе с ней. Цифры в куске есть, шума
  рядом тоже — прочитает ли их движок, скажет прогон.
- **Проверить без устройства** можно только чистую половину: геометрию, выбор области, пороги и
  отказ (`MeterDisplayTest` — синтетические пиксели на **настоящих размерах** кадров корпуса).
  Само распознавание цифр на устройстве и цена подготовки в секундах измеряются живым прогоном.

#### Тот же поиск на остальных 20 кадрах: на чём он срабатывает ещё (ревью)

Три кадра счётчиков говорят только половину. Контрольный прогон пустил тот же поиск по **всем
23 кадрам** и померил, на чём он срабатывает **кроме** приборов:

| что мерили | результат |
|---|---|
| кадров, где место «похожее на табло» нашлось | **22 из 23** (на 22 — полный лимит в три места) |
| верхняя оценка среди всех кадров | **0,196** — фото накладной (13), затем **0,194** рукописная ведомость (19) |
| верхняя оценка настоящих счётчиков | 0,178 (09), 0,117 (15), 0,115 (17) — **ниже двух документов** |
| что оказывалось в верхнем месте | логотип `monobank` на квитанции (20), строка письма «…but it has an error» (07), ряд рукописных дат (19), полоса гравия (13), надпись `GIDROTEK` на крышке водомера (15) |

Три вывода, которые из этого следуют:

- **«Нашли место» ничего не говорит о том, что на кадре прибор.** Порога по `score` тоже нет: он
  отрезал бы счётчики раньше документов.
- **Отказ «табло не найдено» на живом кадре почти не встречается** — значит человек в норме
  видит «нашли, но цифры не читаются», и понимать это как «кадр не про прибор» нельзя.
- **Поэтому чтение прибора не может стоять внутри цепочки «Распознать текст»** (там оно
  подменяло бы облачное чтение документа выдуманным числом) и вынесено в собственный тап —
  разбор в `DECISIONS.md`, «Контрольный прогон: почему звено стало тапом».

**Результат по 15/17 неустойчив к шагу уменьшения.** Тот же кадр и тот же рабочий размер, разница
только в фильтре (билинейный против бикубического) — и на кадре 15 барабан пропадает из всех трёх
мест, а верхними становятся `GIDROTEK` и надпись на кольце; на 17 меняется верхнее место. Значит
таблица выше описывает конкретный путь уменьшения, а не алгоритм; на устройстве путь третий
(`Bitmap.createScaledBitmap`), и его ответ известен только после живого прогона.

### Замер таблицы кадра 23 — где мы 02.08.2026

Шесть живых прогонов «В Excel» за ночь на одном и том же кадре (ведомость владельца, 4000×3000,
3.2 МБ) — с цифрами, потому что без них ни одно из решений этой ночи не было бы принято.

| прогон | время | строк (док. 35) | лишних | сверено/совпало | молча | помечено ⚠ |
|---|---|---|---|---|---|---|
| до правок | **не дошёл за 25 мин** | — | — | — | — | — |
| очередь моделей почищена | 3 мин 25 с | 52 | 13 | — | — | 39 из 439 |
| + разбор ложных пометок | 62 с | 49 | 13 | 35 из 36 (97%) | 1 | 2 из 403 |
| одно чтение (второе не пришло) | 77 с | 34 | 0 | 33 из 36 | 3 | 0 |
| + «отсутствие не согласие» | 92 с | 57 | много | 31 из 36 (86%) | **0** | **402 из 533 (75%)** |
| + «не сошлись — не смешиваем» | **46 с** | **34** | **0** | **35 из 36 (97%)** | **1** | 0 |

Что читается из таблицы и не читается из отдельных чисел:

- **Главная плата была не за качество, а за мёртвую очередь.** Первый прогон не кончился вовсе
  не потому, что модель плохо читает, а потому что перед работающей стояли четыре мёртвых
  провайдера, и каждый брал свой таймаут. Двадцать пять минут → сорок шесть секунд, и ни одна
  из этих правок не про распознавание.
- **Пометки спорности можно снизить до нуля и этим всё испортить.** Пятый прогон — ровно ноль
  молчаливых расхождений, идеальная честность: и 75% залитых ячеек, то есть задание проверить
  таблицу вручную. Метрика назвала это провалом своим словом. Шестой прогон — тот же ноль
  лишних строк и одно молчаливое расхождение, но человек получает таблицу, а не задание.
- **Метрика начала судить только тогда, когда эталон сверили глазами.** До этого «сверено ячеек
  0» — и любой файл с правильными номерами строк проходил без замечаний.

Остаток названного: одно молчаливое расхождение (#345) — в ячейке `11025` напечатано `0,230`,
поверх ручкой `1,0`, и модель отдала только рукописное. В соседних ячейках того же кадра оба
числа доезжают вместе, поэтому это непоследовательность, а не решение.

### Третий замер (02.08.2026, эмулятор, впервые все 23 кадра)

Ночной прогон всего корпуса — впервые вместе с ведомостью (кадр 23) — и правки той же ночи по
его следам. Мерилась первая величина, «справился сам, без облака»: **3 из 8**. Важнее числа —
состав: рост с 2 до 3 дала не новая схема, а закрытая поимённо причина второго замера.

| кадр | действие | итог | почему |
|---|---|---|---|
| 11 | отследить отправление | ✅ | как и в прошлых замерах: трек прочитан правилом |
| 13 | отследить отправление | ✅ | **заработал**: номер накладной напечатан через косую и прочитан дословно целиком — живой кадр подтверждает форму со структурным разделителем (#328). Окно подписи (#350) этим кадром не проверено: номеру с косой подпись не нужна — без строки-подписи он читается так же (`IdentifiersTest`) |
| 22 | построить маршрут | ✅ | как и раньше: координаты прочитаны правилом |
| 09 | показание счётчика | ❌ | чтение кадра не завершилось вовсе — см. ниже |
| 15 | показание счётчика | ❌ | то же |
| 17 | показание счётчика | ❌ | движок кадр дочитал, но показание из шумных атомов офлайн не собралось; путь кадра остаётся облачным, за тапом |
| 12 | построить маршрут | ❌ | строка с адресом в чтении кадра есть, фактом адреса не стала — адресного правила (#350) в билде прогона ещё не было. На дословном тексте этого же прогона правило адрес читает; судить его будет следующий прогон |
| 14 | построить маршрут | ❌ | строка места обрезана экраном — офлайн здесь бессилен по построению (названо во втором замере) |

Строки 12 и 13 объединяет календарь, и он назван, чтобы таблица не судила код, которого в
прогоне не было: кадры 01–22 пройдены в 18:38–19:52, а правки той же ночи — окно подписи и
адресное правило (#350), кроп табло (#352) — написаны в 22:18–22:44, через три часа после
своих кадров. Прогон судит только тот код, который в нём ехал; первый живой судья этих правок —
следующий прогон.

Вне счёта — 15 кадров из 23: к четырнадцати прежним добавилась сама ведомость 23, и это не
потеря — её действие меряется собственной метрикой таблиц («Как это меряется», выше), а не долей
готовности. Знаменатель 8 — состояние на ночь прогона: три схемы «деньги, квитанция и ответ»
(раздел ниже) влились уже после него и поднимают измеримость до 14 — судить их будет следующий
прогон, а не этот.

#### Гейт мусора прятал начисто прочитанную таблицу (кадр 06) — починено

Окно учётной программы с таблицей договоров движок прочитал начисто: 66 слов, уверенность 0,81.
Человек не увидел из этого ничего — гейт мусора объявил кадр рукописью, и документ остался
«просто фотографией». Причина в признаке: читаемость судилась долей **букв** среди символов, а
этот документ состоит из номеров, дат и сумм — доля букв 0,39. У символьной каши с фотографии
ведомости доля букв при этом 0,62: по такому признаку чистое чтение и каша стоят **наоборот**.
Починено (#351): читаемость судит уверенность самого движка — медиана по непустым атомам (0,81
против 0,35 на тех же двух кадрах), и цифра считается прочитанной наравне с буквой. Единственный,
кто знает, читал он или угадывал, — сам движок; доля букв была догадкой поверх его ответа.

#### Чтение на 09, 10, 15 не завершается вовсе — названо, не закрыто

Во втором замере кадры приборов хотя бы дочитывались до символьного шума. В этом прогоне на
кадрах 09, 10 и 15 строка `OCR done` в логе не появилась совсем — журнал остался с одним фактом
приёма; та же картина на 18 и 23 (оба вне счёта). Для метрики это два кадра из восьми (09, 15),
проигранные не правилом и не движком, а тем, что чтение не дошло до конца. Разделу «Подготовка
кадра прибора» этот прогон ответа — прочитает ли движок вырезанное табло — не дал и дать не
мог: сам кроп (#352) в билд прогона ещё не попал (календарь выше). Общий признак всех пяти —
большие фотографии, которым положена тяжёлая подготовка (доворот, поиск табло); это
наблюдение, а не разбор. Причина остаётся названной и
открытой: «не успел» и «не смог» — разные ответы, и следующий прогон обязан их различить.

Помеха инструмента, чтобы отчёту можно было верить: в середину сырого отчёта прогон случайно
вписал промежуточный блок счёта — с «журнала нет» у кадров, до которых сам ещё не дошёл. Ошибка
инструмента названа; итоговое число считает отдельный счёт по всем журналам после прогона, а не
строки из середины отчёта.

#### Опыт «ансамбль против одного Gemini» (кадр 23, вне устройства)

Вопрос владельца: даст ли несколько бесплатных чтецов прирост против одного Gemini. Опыт шёл вне
устройства, но сводил чтения и считал числа механизмами продукта (`reconcileTables`,
`scoreTable`) — свод, переписанный в скрипте, мерил бы качество скрипта. Чтения идут параллельно,
поэтому ансамбль стоит столько времени, сколько самое медленное чтение (здесь — сам Gemini,
90,5 с), и все чтецы — бесплатные квоты. Разбор решений — в `DECISIONS.md` (#346); здесь — числа.

**Первый заход: потолок уже достигнут.** Один бесплатный Gemini на полном кадре прочитал
ведомость начисто — 49 сверенных ячеек из 49, 27 строк из 27, ни одной лишней, включая
рукописные правки поверх печати. Ансамбли повторили то же, добавив разве что лишние строки:

| конфигурация | чтений | совпало из 49 | молча | лишних строк |
|---|---|---|---|---|
| один Gemini (как сейчас) | 1 | 49 | 0 | 0 |
| Gemini ×2 (одна семья) | 2 | 49 | 0 | 1 |
| два поставщика | 2 | 49 | 0 | 0 |
| три поставщика | 3 | 49 | 0 | 0 |
| один Gemini, три варианта кадра | 3 | 49 | 0 | 4 |
| смесь: варианты кадра + поставщики | 4 | 49 | 0 | 0 |

Чтецы при этом не равны: на 35-строчном документе mistral-medium выдал 130 строк, pixtral — 220,
gemini-flash — 35 (выпрямленный кадр утихомиривает mistral до 41). Согласию это не мешает — но и
прибавить к 49 из 49 нечего.

**Тот же конфиг через час — 47 из 49.** Серия «где теряется качество» началась с контрольного
чтения тем же одним Gemini того же полного кадра — и получила 47 вместо 49:

| вариант кадра / запроса | совпало из 49 | молча | строк потеряно |
|---|---|---|---|
| полный кадр, обычный запрос | 47 | 2 | 0 |
| ужат до 3072 (как телефон) | 36 | 13 | 1 |
| ужат до 2048 | 45 | 4 | 0 |
| полный кадр + указатель на слова | 40 | 9 | 0 |
| ужат до 3072 + указатель на слова | 43 | 6 | 1 |

Дороже всего в этой серии стоило ужатие «как телефон»; подсказка со словами движка не помогла и
полному кадру. Но главный итог другой: разброс между одинаковыми прогонами (49 и 47) сравним с
разницей между конфигурациями — одиночный замер вариантов не доказывает ничего, и сам вопрос
меняется с «поднимает ли ансамбль потолок» на «поднимает ли он пол».

**Повторы: разброс 23..48, и худший раунд у всех общий.** Каждая конфигурация прочитана
несколько раз; сверенных ячеек всюду 49:

| конфигурация | раундов | совпало худш/средн/лучш | молча худш/средн/лучш | лишних средн | помечено ⚠ средн |
|---|---|---|---|---|---|
| один Gemini | 2 | 23 / 35,5 / 48 | 26 / 13,5 / 1 | 0,5 | 0 |
| Gemini дважды (сам с собой) | 3 | 23 / 39,7 / 48 | 26 / 9,3 / 1 | 1,3 | 20,7 |
| Gemini + Mistral | 3 | 23 / 36,3 / 48 | 26 / 12,3 / 0 | 0,7 | 61 |
| три поставщика | 3 | 23 / 36,3 / 48 | 26 / 11,7 / 1 | 0,3 | 21,3 |
| четыре чтения | 3 | 23 / 37 / 48 | 26 / 11 / 1 | 0,3 | 47,7 |

Одно и то же чтение тем же чтецом — то 48 из 49 с одной молчаливой ошибкой, то 23 из 49 с
двадцатью шестью. И ансамбль от плохого раза **не защитил**: худший раунд каждой конфигурации —
те же 23 из 49 с теми же 26 молчаливыми, потому что наше правило «чтения не сошлись — не
смешиваем» оставляло первое чтение, то есть выбирало по порядку провайдеров. При таком разбросе
«первый по списку» — монетка. Опыт, затеянный ради прироста, нашёл дыру в нашем собственном
своде.

**Арбитраж по согласию (#346).** Кого оставить при несостоявшемся своде, теперь решает согласие,
а не очередь: остаётся чтение, ближайшее к остальным по значениям (медоид). Это не «лучшее» —
лучшее без эталона недоказуемо, — а единственный имеющийся довод; при двух чтениях довода нет
(расстояние симметрично), там по-прежнему решает порядок. Несостоявшийся свод назван вслух:
сколько чтений реально стоит за таблицей, уходит в метаданные (`confirmedBy`) — раньше человек
получал одно ничем не подтверждённое чтение с видом проверенного, без единой пометки, потому что
спорить было не с кем.

Вывод опыта — не «ансамбль читает лучше» (потолок 49 из 49 берёт и один Gemini), а **«второе
мнение защищает от плохого раза»** — и работает эта защита только через арбитраж по согласию.
Цена согласия видна в той же таблице: у ансамблей в среднем 20,7–61 помеченная ячейка против
нуля у одиночного чтения, и один раунд пары Gemini + Mistral задел порог стены предупреждений
(118 из 333, 35%). Поднялся ли пол после правки — скажет следующий повтор, а не этот: здесь
записано то, что измерено до неё.

### Расширение измеримости: деньги, квитанция и ответ (02.08.2026)

Семь кадров корпуса не судились **ничем**: у их действий не было схемы готовности, и они лежали в
`unscored` рядом с таблицами. Три схемы закрывают шесть из них — и приходят по тому же правилу
приёма, что маршрут и счётчик: **вместе с фактом, которым считается готовность**.

| Действие | Схема | Критические поля | Якорь | Откуда факт |
|---|---|---|---|---|
| Перевести по реквизитам (02, 03) | `pay-by-requisites` | карта **и** сумма | карта | `entity.card` (извлекатель) + новый `entity.amount` (правило `amountFacts` + ключ контракта `AMOUNT`) |
| Переслать квитанцию (08, 20) | `forward-receipt` | номер квитанции **или** ссылка | номер квитанции | новый `entity.receipt` (правило `receiptFacts` + ключ `RECEIPT`) + `entity.url` |
| Ответить (05, 07) | `reply` | адрес почты | тема письма | `entity.email` (извлекатель) + новый `entity.subject` (только модель, ключ `SUBJECT`) |

Измеримых кадров становится **14** (было 8), `unscored` — **9** (было 15). Главное здесь не рост
числа, а то, что у каждого оставшегося кадра теперь есть **названная причина**: семь из девяти —
таблицы, которые меряются результатом действия (`scoreTable`), и два — отказ, записанный вслух.

Числа этих схем ни один прогон ещё не судил: они влились после третьего замера, и первым их
посчитает следующий.

#### Двум кадрам схемы не будет, и это решение, а не забывчивость

- **16 — «собрать список в текст»** (карточка списка покупок в чужом приложении). Честного факта
  «в документе есть список» не существует: галочки OCR не читает вовсе, а «много коротких строк»
  есть и у меню, и у оглавления. Это ровно та же порода, что «в документе есть таблица», и ответ
  тот же — мерить результатом действия, а не готовностью полей.
- **21 — «найти и забрать нужный отчёт»** (дерево еженедельных отчётов на чужом экране). Здесь нет
  не факта, а самого действия: **какой отчёт нужен, знает человек**, на странице этого нет ни в
  каком виде. Схема, объявляющая кадр готовым за прочитанное имя файла, мерила бы наше чтение и
  называла бы это выполненным действием.

#### Что названо вслух вместе со схемами

- **Правило суммы не отличает цену от суммы к переводу.** На кадре 02 стоят обе; правило пишет
  первую, вторая честно видна в «или: …». Улика ровно одна (`semantic`), человек видит «возможно».
  Проверено на дословном выводе устройства (`ocr/neg_viber.txt`): правило читает ровно две суммы
  кадра и ни одного обрезка шестнадцатизначной карты, стоящей двумя строками выше.
- **Кадр 03 офлайн не закрывается.** Сумма к оплате там живёт внутри расчёта («127*4.32=548,64»),
  валюты рядом нет ни у одной — правило молчит честно, путь к факту один: «Понять» (ключ `AMOUNT`).
  *Записано 02.08.2026 и с 03.08.2026 неверно:* правило читает такую сумму само — итогом
  сошедшегося расчёта (раздел «Кадры 03 и 20»). Строка оставлена как запись того дня.
- **Кадр 08 карточку не покажет.** Квитанция там за ссылкой, номера квитанции на экране нет, а
  ссылка якорем не сделана намеренно: она есть на каждом втором скриншоте. Измеримости это не
  касается — метрика считает `readiness()`, где якоря нет (та же цена, что у адреса в маршруте).
  Сама ссылка на кадре к тому же **обрезана экраном** — прогон покажет, дошла ли она до факта.
- **Кадр 05 карточку не покажет тоже:** тема письма там буквально «(no subject)».
- **Номер карты на экране показывается хвостом.** Схема перевода сделала карту ключевым значением
  готового действия, то есть строкой, которую карточка печатает первой. Маскировка переехала к
  ключу факта (`maskedForScreen`) и стала общей для всех экранов — прецедент #240, где маска
  работала на одном экране, а номер утёк мимо неё другим полем.
## Кадр 18 (03.08.2026): эталон сошёлся с документом, а чтение — нет

Ночной прогон таблицы кадра 18 (сводная таблица к счёту, лист снят вверх ногами и смят по сгибу)
отчитался так: **строк в документе 2, в файле 4; колонок 7 против 12; найдено 2 из 2, лишних 2;
проверено 10 ячеек, совпало 1**. «Строк в документе 2» на сводной таблице выглядит подозрительно
мало, поэтому первым подозреваемым был эталон.

**Эталон верен.** Кадр пересмотрен глазами по кускам целиком: строк данных ровно две, строки
заголовков нет (сетка начинается сразу под строкой «накладная: …»), колонок семь, и все десять
сверенных значений стоят на бумаге так, как записаны. Узкой пустой колонки между наименованием и
числом без подписи, которая мерещится на общем виде, в сетке нет — это сгиб листа. Запись о
перепроверке — в шапке самого `tools/corpus/18.expected.tsv`: эталон правят по документу, а не по
тому, что оказалось трудно прочесть.

Расходится с документом **чтение**, и у расхождения две названные причины.

- **Четыре строки вместо двух — свод не узнал свои же строки.** Оба чтения прочитали одни и те же
  две строки, но разложили их по-разному: одно указало на каждое слово отдельной ячейкой (12
  ячеек в строке), другое собрало слова в ячейки (6). Строки узнают друг друга по совпадению
  ячеек **на тех же местах**, а ключевая колонка (артикул) требует, чтобы он стоял в колонке и
  чтобы строк было не меньше пяти, — на двухстрочном документе с разной разбивкой закрылись оба
  пути сразу. Обе версии обеих строк уехали в файл: каждая строка документа напечатана дважды, и
  ни одной пометки об этом. **Починено:** строку опознаёт её идентификатор, где бы в строке он ни
  стоял (`core/flow/TableConsensus.kt`, разбор — в `DECISIONS.md`).
- **Одиннадцать предупреждений стёрло оформление.** Приложение насчитало 24 помеченные ячейки, а
  в файле их осталось 13: «⚠» живёт заливкой, а не текстом, и стиль шапки стоял на первой строке
  безусловно — у документа без заголовков первая строка данных красилась серым «заголовком»
  вместе со всеми своими пометками. **Починено:** пометка неуверенности оформлению не уступает
  (`data/OoxmlSpreadsheetWriter.kt`).

**Чего эти правки не чинят — сказано вслух, потому что хорошим число по кадру они не сделают.**
Ширина остаётся не той (12 колонок против 7): её задаёт чтение, указавшее на каждое слово
отдельной ячейкой, а это поведение модели, а не наш свод. Значения тоже разошлись: у второго,
«человеческого» чтения три последние колонки идут в обратном порядке (упаковка → количество →
единица вместо единица → количество → упаковка), а колонка «число без подписи» (86205 и 9)
потеряна целиком. Движок телефона на этом кадре читает плохо и сам по себе — `86205` он отдал как
`36205`, а `кв.м.` и `42` как `is` и `2`. Насколько сдвинутся числа кадра, скажет следующий
прогон, а не автор правки.

## Кадры 03 и 20 (03.08.2026): не хватало прочитанного, а не обещанного

Прогон записал «5 из 14», и оба новых измеримых кадра про деньги оказались среди несправившихся.
Разбор журналов и самих кадров глазами дал один и тот же ответ на оба: **критическое поле есть на
странице, и его не нашло правило.** Схема не менялась ни в одном поле — разбор целиком, вместе с
названными ценами, в `DECISIONS.md` («Два провала корпуса были чтением, а не схемой»).

| кадр | чего не хватало | что на самом деле на странице | почему не читалось |
|---|---|---|---|
| 03 | суммы к переводу | `500+548,64=1048,64` — итог напечатан дословно | правило искало число рядом со знаком валюты, а её на переписке нет ни одной |
| 20 | номера квитанции | «Квитанція № …» в заголовке листа | движок отдал слово как «Квитанщя», слепив «ці» в одну букву, и допуска по соседнему слову не стало |

Две вещи, которые сделаны **не** были, и это часть результата:

- **Схема не подгонялась под кадр.** Сумма осталась критической у «Перевести по реквизитам»
  (без неё человек дописывает её сам — то есть действие выполнено не без правок), а требование
  «сама квитанция, а не снимок приложения» осталось критическим у «Переслать квитанцию».
- **QR кадра 20 не был объявлен ссылкой на квитанцию.** В журнале он лежит и выглядит как готовое
  второе чтение, но на листе это рекламный QR («Отримайте картку … за QR-кодом»). Действие
  позеленело бы, а человек переслал бы получателю рекламу.

Числа прогона задним числом не переписываются: сдвинулись ли эти два кадра, скажет следующий
прогон на устройстве.

### Четвёртый замер (03.08.2026, эмулятор, впервые все 14 кадров со схемой)

Первый прогон после того, как в `main` влились правки чтения (#262, #266, #279, #288, #358).
Мерилась первая величина, «справился сам, без облака»: **7 из 14**. Знаменатель впервые полный —
прогон шёл ровно по кадрам, у которых схема есть, а девять кадров вне счёта не запускались вовсе
(семь табличных и два с названным отказом), поэтому «не проверялись» в отчёте — это они.

| кадр | действие | итог | почему |
|---|---|---|---|
| 02 | перевести по реквизитам | ✅ | карта, сумма и трек прочитаны правилом |
| 03 | перевести по реквизитам | ✅ | **сдвинулся**: сумма `1048,64` собрана из напечатанного расчёта — та самая правка «искали число у знака валюты, а его на переписке нет» |
| 05 | ответить | ✅ | впервые в счёте: схема «ответ» влилась после третьего замера, кадр берётся офлайн |
| 08 | переслать квитанцию | ✅ | впервые в счёте: ссылка на квитанцию прочитана с экрана банка |
| 11 | отследить отправление | ✅ | как и в прошлых замерах |
| 20 | переслать квитанцию | ✅ | **сдвинулся**: номер квитанции прочитан, хотя движок пишет слово с дефектом — допуск по соседнему слову сработал |
| 22 | построить маршрут | ✅ | координаты прочитаны правилом |
| 07 | ответить | ❌ | чтение кадра не собралось: движок отдал кашу, гейт мусора её не пропустил — текста у объекта нет вовсе |
| 09 | показание счётчика | ❌ | то же, что и в третьем замере: офлайн показание не собирается |
| 12 | построить маршрут | ❌ | чтение кадра не собралось — адресному правилу нечего читать |
| 13 | отследить отправление | ❌ | **регресс против третьего замера**, где кадр был зелёным: тогда номер накладной прочитался дословно, теперь текст не собрался вовсе. Причина не разобрана; прогон судит то, что было в нём |
| 14 | построить маршрут | ❌ | строка места обрезана экраном — офлайн бессилен по построению (названо во втором замере) |
| 15 | показание счётчика | ❌ | как 09 |
| 17 | показание счётчика | ❌ | как 09 |

Два кадра, которым предыдущий раздел обещал ответ «следующего прогона», ответ получили: **03 и
20 сдвинулись** — правки чтения подтвердились живым устройством, а не только тестом.

#### Побочная находка: «Прочитать показание» предлагалось на всём подряд

Прогон записал `reading.mode` на каждом кадре, и по нему видно то, чего не видно из числа:

| режим | кадры | что это |
|---|---|---|
| `PRINTED` | 02, 03, 05, 08, 11, 14, 20, 22 | переписки, письма, квитанции, посылки — приборов нет ни одного |
| `HANDWRITTEN` (страница не прочиталась) | 07, 09, 12, 13, 15, 17 | сюда попали **все три счётчика** |

`MeterOcrCapability.accepts` при этом брал любое изображение, то есть пузырёк чтения табло висел
и на письме, и на квитанции. Замечание владельца («не нужно показание на всём подряд») этим
разрезом и меряется: правило «показывать только там, где страница печатным текстом не
прочиталась» снимает пузырёк с восьми кадров из четырнадцати и не теряет ни одного прибора.

### Пятый замер (04.08.2026, эмулятор, main до правок #493)

Владелец сказал: приоритет на качество, — и двое взялись чинить таблицы (#493). Отправной точки,
с которой потом сравнят «после», не было ни у кого: **никто не знал, насколько плохо сейчас**.
Этот замер её ставит. Прогон шёл по `main` (db6fe33) **до** правок #493, в отдельной ветке,
логику чтения не трогая.

Впервые меряются **четыре** величины вместо трёх: к правде таблицы добавилась её годность
(«Как это меряется» → «Годность файла»).

**Правки #493 приехали в `main` в тот же день, пока шёл прогон** (`0ed8f41` в 14:25, `d3e6214` в
15:40, `9ed99b3` в 16:34 — чтение лучшим читателем, развязка текста с атомами, обломанный ответ
модели). Замер их **не судит**: прогон целиком шёл на сборке `db6fe33`, снятой до первой из них.
Именно это и делает числа ниже отправной точкой — их значение в том, что они «до».

**Отдельно про имена, чтобы не спутали два разных механизма.** В `main` в тот же день появился
`TableFitness` — это **ворота продукта**: они решают, отдавать ли человеку файл вообще, и считают
по внутренним признакам (сколько слов не легло ни в одну часть, на скольких ячейках спорят
чтения). Здесь заведена **мера замера**: она считает по уже отданному `.xlsx` то, что видит
человек, — и потому у неё другой вход (готовый файл), другой знаменатель (непустые ячейки листа) и
своё правило символьного шума, которого воротам знать неоткуда. Общий у них один порог — треть
пометок, — и он общий сознательно: продукт обязан отказывать ровно там, где замер уже говорит «это
не результат».

#### На чём мерили и чего это стоит числам

Эмулятор `Medium_Phone_API_36.1`, сборка отладочная, с ключами. **Телефон владельца в это время
занят другой работой** (на нём стоит чужая сборка), и ставить поверх свою значило бы отобрать
устройство у соседа — поэтому живой телефон в этом замере не участвовал вовсе.

Цена этого выбора названа, а не спрятана: **в APK нет x86_64** — только `arm64-v8a` и
`armeabi-v7a`, — поэтому на x86-эмуляторе движок чтения идёт через трансляцию и работает в разы
медленнее телефона. Отсюда две поправки к числам ниже:

- страницы, которые телефон читает за секунды, здесь читаются **на пределе бюджета** (180 с,
  `OCR_READ_BUDGET_MS`), а самые тяжёлые в него упираются: первая попытка кадра 23 вернула
  `0 слов, 0 символов, 181 837 мс (timeout)`;
- значит **числа чтения на эмуляторе — нижняя граница, а не оценка движка**; «на устройстве
  владельца было бы так же» из них не следует.

Секунды прогона по той же причине здесь не сравнимы с прежними записями и не приводятся.

#### Таблицы: пять размеченных кадров, две величины рядом

Каждый кадр прошёл живой путь целиком: расшарен в Point, прочитан на устройстве, тапнуто
«В Excel», подтверждено согласие на облако, забран сам `.xlsx`.

| кадр | что за документ | прочитал сам | строк док/файл | колонок | сверено/совпало | молча | ⚠ в файле | **годен как есть** |
|---|---|---|---|---|---|---|---|---|
| 06 | окно учётной программы | 143 слова | 16 / 16 | 7 / 7 | 24 / **24** (100%) | **0** | 7 из 104 | **93%** ✅ |
| 01 | техкарта внутри снимка экрана | 36 слов | 12 / 3 | 7 / 6 | 25 / **0** (0%) | **25** | 0 из 21 | 75% ❌ свалка |
| 18 | сводная таблица, лист вверх ногами | 69 слов | 2 / 2 | 7 / 9 | 10 / **1** (10%) | **5** | 12 из 26 | 44% ❌ шум + стена |
| 04 | та же техкарта, снята целиком | 385 слов | 12 / 3 | 7 / 6 | 25 / **0** (0%) | **25** | 22 из 27 | **19%** ❌ шум + стена |
| 23 | продовольственная ведомость владельца | 427 слов | 35 / 34 | 9 / 9 | 49 / **36** (73%) | **3** | 213 из 261 | **18%** ❌ стена |

Из пяти файлов **верен один** (06) и **годен один** (06). Это и есть отправная точка.

Что читается из таблицы и не читается из отдельных чисел:

- **Кадр 23 — тот самый сценарий владельца**, и число его «бедовости» теперь есть: годных ячеек
  **18%**. Сетка при этом собралась почти идеально (34 строки из 35, ширина сошлась, ни одной
  лишней), значения в основном верные (36 из 49) — а работать нельзя, потому что **помечено 213
  ячеек из 261**. Человек открыл не таблицу, а задание перепроверить её целиком. Величина правды
  этот файл почти хвалит, величина годности — называет провалом; вместе они говорят точно то, что
  сказал владелец.
- **Кадр 01 — доказательство, что одной величины мало.** Файл выглядит чистым: ни одной пометки,
  ни одного шумного знака, годность 75%. И он **выдуман целиком**: 25 сверенных ячеек из 25
  разошлись с документом, ни одна не предупредила. В файл уехала аккуратная накладная с круглыми
  числами, которой на странице нет вовсе, а настоящие слова страницы лежат ниже, под подписью
  «Непрочитанное». Красивая ложь получает высокую годность — поэтому числа печатаются парой.
- **Кадры 01 и 04 — один и тот же документ, две крайности.** На 01 страница почти не прочиталась
  на устройстве (36 слов) — и вышла чистая выдумка. На 04 та же техкарта прочиталась хорошо
  (385 слов) — и вышла честная каша: 30% ячеек с символьным шумом, 22 пометки из 27, годность 19%.
  Это ровно тот шов, который чинит #493: сегодня выбор стоит между «красиво и молча неверно» и
  «правдиво и непригодно».
- **Кадр 06 показывает, что дело не в потолке.** Начисто прочитанное окно программы даёт 100%
  сверенных ячеек, ноль молчаливых расхождений и 93% годных ячеек. Значит плохие числа выше — не
  свойство задачи «таблица», а свойство тяжёлых входов и сегодняшней сборки текста.

#### Контрольный замер: тот же кадр 23, но страницу устройство не прочитало

Первая попытка кадра 23 упёрлась в бюджет чтения (`0 слов`), и файл собрался **без привязки к
словам страницы** — то есть так, как его продиктовала зрячая модель. Числа этого файла:

| | сверено/совпало | молча | ⚠ | шум | **годен как есть** |
|---|---|---|---|---|---|
| со словами страницы (обычный путь) | 49 / 36 | 3 | 213 из 261 | 0 | **18%** |
| без слов страницы (страница не прочиталась) | 49 / 28 | **21** | **0** | 0 | **100%** |

Один и тот же кадр, один и тот же билд: с привязкой — правда ценой негодности, без привязки —
идеальная на вид таблица, которая молча врёт в двадцати одной ячейке из сорока девяти. Ни один из
двух исходов не является результатом, и это самый прямой довод в пользу того, что чинится в #493.

#### Что пришлось починить в харнессе, чтобы замер вообще состоялся

Инструмент отстал от продукта, и молчал об этом провалами, похожими на провалы продукта:

1. **Вопрос «Отправить в облако?» (#468) харнесс не знал.** Он тапал «В Excel» и ждал файл,
   которого не будет никогда: объект стоял за согласием человека. В отчёт это попадало как
   «действие до файла не дошло» — то есть как отказ продукта. Теперь согласие подтверждается и
   **пишется строкой в отчёт**: замер без этой строки выглядел бы так, будто объект уехал в облако
   сам.
2. **Помеха эмулятора убиралась раньше, чем искалась кнопка.** Диалог успевал закрыться сам, и тап
   по запомненному месту приходился уже по экрану приложения — в замере он открыл «Дать ссылку», а
   дальше харнесс шесть раз искал «В Excel» на чужом экране и записал «кнопка не найдена». Теперь
   помеха убирается только когда кнопки нет, и по собственному свежему снимку экрана.
3. **Ожидание файла досиживало предел до конца**, даже когда действие уже кончилось отказом
   («на странице не нашлось ничего, что можно положить в таблицу»): полчаса на кадр, а в отчёте —
   тот же ответ, что у не успевшего. Теперь харнесс спрашивает экран, идёт ли ещё действие.
4. **Предел ожидания чтения был зашит числом** (200 с). Стал переменной `OCR_WAIT`: скорость
   машины к качеству чтения отношения не имеет, а «не успел» и «не смог» — разные ответы.

#### Первая величина: 2 из 14, и это число прежде всего про стенд

Тот же прогон померил и «справился сам, без сети»: **2 из 14**. Число резко ниже прошлого
(7 из 14, 03.08.2026), и прежде чем читать его как ухудшение продукта, надо прочитать его состав.

| кадр | что человек хочет | страницу прочитал | текст стал объектом | итог | почему |
|---|---|---|---|---|---|
| 20 | переслать квитанцию | 113 слов | да | ✅ | номер квитанции прочитан |
| 22 | построить маршрут | 60 слов | да | ✅ | координаты прочитаны |
| 02 | перевести по реквизитам | 96 слов | да | ❌ | сумма и номер отправления есть, **карты нет** |
| 03 | перевести по реквизитам | 52 слова | да | ❌ | сумма собрана из расчёта, **карты нет** |
| 05 | ответить | 59 слов | да | ❌ | адрес почты фактом не стал |
| 08 | переслать квитанцию | 45 слов | да | ❌ | ни номера, ни ссылки — ссылка на листе срезана экраном |
| 14 | построить маршрут | 96 слов | да | ❌ | строка места обрезана экраном (названо во втором замере) |
| 09 | передать показание | 308 слов | **нет** | ❌ | прочитанное не прошло гейт мусора |
| 12 | построить маршрут | 105 слов | **нет** | ❌ | то же |
| 13 | отследить отправление | 176 слов | **нет** | ❌ | то же |
| 15 | передать показание | 135 слов | **нет** | ❌ | то же |
| 07 | ответить | **0 слов, предел 180 с** | нет | ❌ | чтение не уложилось в бюджет |
| 11 | отследить отправление | **0 слов, предел 180 с** | нет | ❌ | то же — а это исторически самый зелёный кадр корпуса |
| 17 | передать показание | **строки нет за 400 с** | нет | ❌ | чтение не кончилось вовсе |

Что из этого следует и что не следует:

- **Три кадра (07, 11, 17) проиграли не продукту, а стенду.** Это те же большие фотографии, что
  упираются в бюджет чтения на транслируемом движке; кадр 11 в прошлых четырёх замерах был зелёным
  всегда. Ещё четыре кадра (09, 12, 13, 15) потеряли текст на гейте мусора — их страницы
  прочитались, но объектом прочитанное не стало.
- **Четыре кадра (02, 03, 05, 08) читаются, а критического значения не отдают** — и вот это уже
  вопрос к продукту, а не к стенду: 03.08 те же четыре были зелёными. Разобрать здесь, чтение это
  или правило, замер не может: для этого нужен тот же кадр на телефоне владельца, а телефон занят
  другой работой. Поэтому строка записана как **открытый вопрос, а не как обвинение**: числа
  хватает, чтобы заметить, и не хватает, чтобы назвать виновного.
- **Сравнивать 2 из 14 с 7 из 14 напрямую нельзя.** Скорость машины входит в бюджет чтения, а
  значит и в число, — а в этот день на той же машине шли ещё две работы над тем же продуктом. Обе
  записи остаются как есть; какая из них воспроизводится, скажет следующий прогон, желательно на
  живом телефоне.

#### Чего этот замер НЕ измерил

- **Живой путь владельца на его телефоне.** Телефон занят другой работой, и своя сборка затёрла бы
  чужую. Поэтому «сколько ячеек ведомости приходит дословно **на A34**» — не проверено.
- **Свалку из его файла воспроизвести не удалось.** В разборе #493 треть листа владельца ушла в
  «Непрочитанное» (11 строк из 23), а ячейки были в символьном шуме; в нашем файле того же кадра
  непрочитанного — одна ячейка, шума нет вовсе, а негодность даёт стена пометок. Файлы сняты с
  разных устройств, и сравнивать их числа нельзя. Вывод из этого один и он важен: **негодной
  таблица бывает по-разному**, поэтому у меры и названы четыре причины, а не одна.
- **Символьный шум на ведомости здесь не пойман** (0 ячеек из 261): текст в файле кадра 23 чистый,
  негодным его делают пометки. Шум пойман на других кадрах — 04 (30%) и 18 (12%), — то есть само
  правило работает; на этом кадре его просто не на чем было сработать.
