package com.point.core.flow

/** Как модели сказать, что текста нет: одна скобочная строка — форма, которую узнаёт [serviceNote]. */
const val NO_TEXT_CLAUSE: String = "Если текста на изображении нет — ответь ровно: [нет текста]"

/**
 * О чём Point просит зрячую модель, когда читает кадр (#840).
 *
 * Просьба была записана дважды — в действии «Распознать текст» и у облачного читателя, — и
 * это не про красоту: промпт есть контракт с моделью. Разойдись формулировки, и один и тот
 * же снимок читался бы по-разному в зависимости от того, каким путём Point до него добрался,
 * а объяснить это человеку было бы нечем.
 *
 * Снимок без текста — часть контракта (#1054): модель отвечает условленной пометкой, а не
 * прозой вроде «На изображении нет текста», которую не отличить от прочитанного. Пометку
 * узнаёт общий сторож [serviceNote] — той же формой, какой метят своё замечание и готовые
 * сервисы чтения.
 */
const val CLOUD_READING_PROMPT: String =
    "Извлеки весь текст с изображения дословно, сохраняя порядок строк. " +
        "Таблицы оформи в Markdown. Верни только текст, без комментариев. " +
        NO_TEXT_CLAUSE
