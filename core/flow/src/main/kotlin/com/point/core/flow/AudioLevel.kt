package com.point.core.flow

import com.point.core.model.PointObject

/**
 * Уровень звука записи, измеренный на самом устройстве (#1053).
 *
 * Пустая запись не молчит в облаке: двадцать секунд цифровой тишины возвращались из Whisper
 * фразой «Thank you.», и выдумка ложилась знанием об объекте — по ней предлагали «Понять» и
 * «Перевести». Спрашивать сервис, есть ли в записи звук, незачем: у записи, в которой нечего
 * слушать, самый громкий сэмпл лежит у нуля, и это видно здесь, до отправки.
 *
 * Само измерение — side effect (декодер), поэтому живёт за интерфейсом; правило ниже — чистое.
 */
fun interface AudioLevel {

    /**
     * Самый громкий сэмпл записи, 0..1.
     *
     * `null` — измерить не вышло: формат не открылся, декодер отказал, времени не хватило.
     * Это не «тихо»: не измерили — значит, запись идёт дальше обычным путём.
     */
    suspend fun peak(obj: PointObject): Double?
}

/**
 * Измеренный пик — только когда было что мерить (#1053).
 *
 * Ни одного сэмпла не разобрали — измерение не состоялось, и ответ здесь «не измерили»
 * (`null`), а не «тихо». Сорвавшееся измерение никогда не переводит знание в «не нашлось»
 * (ADR-0001 §9): иначе незнакомый формат и отказавший декодер молча отнимали бы у человека
 * расшифровку, объявив пустотой то, чего не слушали.
 */
fun measuredPeak(peak: Double, anySampleHeard: Boolean): Double? = peak.takeIf { anySampleHeard }

/**
 * Измерено самим устройством: звука в записи нет (#1053).
 *
 * Это знание о самой записи, а не ответ одного исполнителя, — и потому оно закрывает вопрос
 * целиком, вместе с очередью исполнителей. Сколько бы их ни стояло за первым, каждый получит
 * те же байты и ту же тишину: уступать им очередь незачем, и `FallbackRealizer` этого не
 * делает — шаг вернулся не с пустыми руками, а с находкой о содержимом.
 *
 * Прежде измеренная тишина приходила одним лишь «не нашлось». Цепочка считала шаг пустым и
 * отправляла запись дальше — на компьютер и оттуда в сервис, за той же выдумкой, ради
 * которой всё и слушалось.
 *
 * Ответ сервиса «речи не слышно» этим знанием не является: сервис сказал про речь, а не про
 * звук, и следующий движок за ним ещё может услышать (#1054).
 */
const val META_AUDIO_SILENT = "audio.silent"

/**
 * Тише этого пика в записи нет звука вовсе.
 *
 * 0,5 % от полной шкалы (около −46 dBFS) — это цифровые нули и шум кодека вокруг них, а не
 * тихий голос: даже далёкая от микрофона речь даёт пик на порядок выше. Порог намеренно
 * низкий: не расслышать настоящую речь дороже, чем впустую сходить в сервис.
 */
const val AUDIBLE_PEAK = 0.005

/** В записи нечего слушать — отправлять её некуда (#1053). */
fun nothingToHear(peak: Double?): Boolean = peak != null && peak < AUDIBLE_PEAK
