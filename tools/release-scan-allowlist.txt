# Release-эталон для tools/release-scan.sh (#306).
#
# Здесь перечисляются ТОЧНЫЕ строки, которые совпадают с секретными формами
# (AIza…, gh[pousr]_…, sk-…), но секретами не являются и законно живут в
# release-артефакте — например, случайная последовательность байт в шрифте или
# нативной библиотеке, распечатавшаяся как «sk-…».
#
# Правила:
#   * по одной строке на запись, сравнение точное (вся строка целиком);
#   * каждая запись — с комментарием строкой выше: откуда она и почему безобидна;
#   * живой ключ сюда не вносится НИКОГДА — он отзывается и перевыпускается;
#   * файл публичен по определению: всё, что в нём, уже лежит в раздаваемом артефакте.
#
# Сейчас эталон пуст: проверенные release-артефакты (APK v0.2.x, desktop-MSI)
# не содержат ни одного совпадения. Появится безобидное — впиши его сюда с причиной,
# и скан снова станет зелёным, не ослабляя правило для всего остального.

# Выдуманный ключ из тестов онбординга ключа (KeyOnboardingTest, AiKeyCheckTest): проверяет, что
# строка ПОХОЖА на ключ OpenRouter. Живым не был никогда — цифры по порядку, длина ровно на
# границе проверки. В артефакт не попадает: тесты не компилируются в релиз.
sk-or-v1-0123456789abcdefghij
# Второй выдуманный ключ того же теста (AiKeyCheckTest): длина ровно на нижней границе проверки —
# по нему судится, что слишком короткая строка ключом НЕ считается. Живым не был никогда.
sk-or-v1-0123456789abcdef0123

# Клиентский ключ Firebase (app/google-services.json, current_key). Плагин google-services вшивает
# его в APK для FCM (стук/push) — это client-side ключ по устройству Firebase, а НЕ серверный
# секрет: в local.properties его нет (скан сверился с 14 значениями — не совпал). В раздаваемом
# артефакте живёт по устройству и безопасен ПРИ УСЛОВИИ, что ключ ограничен в Google Cloud Console
# (пакет com.point + SHA подписи релиза + только нужные API); держать это ограничение — на владельце.
# Исключение из «живой ключ не вносится»: клиентский ключ Firebase ший­ется в приложение штатно.
# Решение владельца 07.09.2026 при подготовке 0.3.7.
AIzaSyB6TpPoNvNxQhj1AEeuajfGnBWR0r44lys
