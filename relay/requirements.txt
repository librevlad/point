# Сервер аккаунтов Point (#471). Зависимостей нарочно мало: всё, что можно сделать
# стандартной библиотекой (HTTP-запрос к Google, SQLite, случайность, SHA-256), сделано ею.
fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.10.4

# Проверка подписи id_token Google (RS256 + JWKS). Своя реализация RS256 была бы худшим
# кодом в проекте — поэтому библиотека. Импортируется лениво: тесты её не требуют.
PyJWT[crypto]==2.10.1

# Разбор формы, которой чужой человек кладёт файл в ящик приёма (#476). На машине разработчика
# библиотека оказалась установлена заодно с чем-то ещё, и приём файла «работал» — а на чистом
# раннере тот же путь падал с требованием её поставить. Зависимость, которую забыли объявить,
# ведёт себя ровно так: молчит там, где её проверяют, и ломается там, где на неё положились.
python-multipart==0.0.20
